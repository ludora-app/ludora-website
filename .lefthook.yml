pre-commit:
  parallel: true
  commands:
    format:
      run: pnpm format --log-level warn && git add -u
      glob: '**/*'
    lint:
      run: pnpm lint:fix .
      glob: '**/*.{ts,tsx}'
    typecheck:
      run: pnpm ts:check --noEmit
